PORT=5000

# xAI (Grok) — required for resume analysis
XAI_API_KEY=
XAI_BASE_URL=https://api.x.ai/v1
XAI_MODEL=grok-3-mini

# Neon PostgreSQL — optional; persistence for analysis runs
# Copy connection string from Neon Dashboard → Connection details
# Run server/db/schema.sql once in the Neon SQL Editor
DATABASE_URL=
